
########################################################################
#                                                                      #
# DO NOT EDIT THIS FILE, ALL EDITS SHOULD BE DONE IN THE GIT REPO,     #
# PUSHED TO GITHUB AND PULLED HERE.                                    #
#                                                                      #
# LOCAL EDITS WILL BE OVERWRITTEN.                                     #
#                                                                      #
########################################################################

{
  hostname = "";
  # No need to import standard modules or users (see configuration.nix).
  # Only extra modules need to be added here.
  imports = [
  ];

  boot = {
    # Set to either "legacy" or "uefi" depending on how you install the system
    mode = "uefi";
    # Set to "nodev" for a uefi system.
    device = "nodev";
  };

  # Timezone of the location where the server will be deployed
  timezone = "Europe/Brussels";
  reverse_tunnel = {
    enabled = true;
    forward_port = "";
  };
  crypto = {
    enabled = false;
    encrypted_device = "/dev/LVMVolGroup/nixos_data";
  };

}

